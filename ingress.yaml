apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: online-shop-service
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: shop-active
            port:
              number: 80